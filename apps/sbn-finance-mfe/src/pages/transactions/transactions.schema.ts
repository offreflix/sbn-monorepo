// Any Zod validations needed for transactions filtering or page logic go here.
// CreateTransactionModal handles its own forms, but any page-specific schemas should be placed here.
export const transactionsSchema = {};
